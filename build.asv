function axe = build(o, x, y)
% o: cross point of x, y vector
% x, y is vector to bu

    x = normalizerow(x);
    y = normalizerow(y);
    z = normalizerow(cross(x, y));
    y = cross(z, x);
    axe = [x', y', z', o'; 0, 0, 0, 1];
end